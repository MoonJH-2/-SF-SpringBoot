spring:
  application:
    name: demo01
  data:
    mongodb:
      host: localhost
      port: 27017
      database: forSpringDB
springdoc:
  api-docs:
    path: /api-docs
  swagger-ui:
    path: /swagger-ui.html

logging:
  level:
    org.springframework.security: DEBUG